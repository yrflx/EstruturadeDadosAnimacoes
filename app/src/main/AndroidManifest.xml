<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yurif.estruturadedados_animacoes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListaSequencial"
            android:label="Lista Sequencial"
            android:screenOrientation="portrait" />
        <activity
            android:name=".LSEActivity"
            android:label="LSE"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PilhaActivity"
            android:label="Pilha"
            android:screenOrientation="portrait" />
        <activity
            android:name=".FilasActivity"
            android:label="Filas"
            android:screenOrientation="portrait" />
        <activity android:name=".ArvoreActivity"
            android:screenOrientation="landscape"
            android:label="Arvore Binaria"

            ></activity>
    </application>

</manifest>